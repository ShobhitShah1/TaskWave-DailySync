// No complex types currently needed as the module uses primitive arguments.
// You can define specific interfaces here if the API evolves to use objects.
export type SendMessageEvents = Record<string, never>;
